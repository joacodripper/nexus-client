# Copia este archivo a `.env` si tu API no corre en http://localhost:8000.
# En Vercel define esta variable en Project → Settings → Environment Variables.
# URL base de la API de Nexus (sin barra final).
VITE_API_URL=http://localhost:8000/api
